species_taxid : "10020"
species_name : "Dipodomys ordii"

# Production settings
debug: true
max_cpus: 4  # Reduced for dev mode
max_memory: '8GB'  # Reduced for dev mode

# Optional: Add these if you want to customize the output directory
output_dir: "Readthrough/test"  # Local output directory
geneid_param: "human3iso.param"  # Update to your geneid parameter file location